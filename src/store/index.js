import Vue from 'vue'
import Vuex from 'vuex'

// Modules
import oauth from './modules/oauth'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    oauth
  }
})
